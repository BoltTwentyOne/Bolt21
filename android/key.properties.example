# Android Release Signing Configuration
# Copy this file to key.properties and fill in your values
# NEVER commit key.properties to source control

storePassword=your_keystore_password
keyPassword=your_key_password
keyAlias=bolt21
storeFile=../secrets/bolt21-release.jks

# To generate a keystore:
# keytool -genkey -v -keystore ../secrets/bolt21-release.jks -keyalg RSA -keysize 2048 -validity 10000 -alias bolt21
